//! Host-side simulation support for device behavior.
//!
//! This crate is intentionally skeletal. It may use host facilities later to
//! exercise core crates without ESP32 hardware.

#[cfg(test)]
mod tests {
    #[test]
    fn smoke_test() {
        assert!(true);
    }
}
